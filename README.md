🌐 Simple Flask API

📌 Overview
This project is a basic REST API built with Flask.
It demonstrates how to create endpoints for retrieving, creating, updating, and deleting user data.
Perfect for learning CRUD operations and testing with Postman.


⚙️ Features
GET /get-user/<user_id> → Returns user information based on the user_id.

POST /create-user → Accepts JSON data to create a new user.

PUT /update-user/<user_id> → Updates user information.

DELETE /delete-user/<user_id> → Deletes a user.

🚀 How to Run
1. Install Flask:
   pip install flask
2. Run the server:
   python app.py
3. Test the endpoints with Postman or your browser.


📚 Learning Objectives
- Practice with Flask and understand how routes work.  
- Learn how to handle dynamic parameters and JSON payloads.  
- Use Postman to test APIs.  

